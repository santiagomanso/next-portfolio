import { certificatesData } from './certificates';
import { labels } from './labels';
import { mediaData, MediaInterface } from './media';

export { certificatesData, labels, mediaData, type MediaInterface };
